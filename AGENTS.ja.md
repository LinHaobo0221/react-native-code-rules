# Codex のリポジトリ共通実行ルール

rules_language: ja

文書へのリンクは、本ファイルがあるディレクトリを基準に解決する。インストール済みパッケージから本ファイルを読む場合も、利用側プロジェクトの作業ディレクトリではなく、パッケージ内の本ファイルの位置を基準とする。

## 1. 適用範囲とルールの優先順位

本ファイルはリポジトリのルートに置かれ、リポジトリ全体に適用される。対象コードにより近いディレクトリにある `AGENTS.md` または `AGENTS.override.md` は、そのディレクトリ内に限り、本ファイルのルールを補足または上書きする。

ルールが競合する場合は、次の優先順位で処理する。

1. 現在のタスクでユーザーが明示した要件、制約、受け入れ条件。
2. 対象ファイルに最も近く、適用範囲内にある `AGENTS.override.md` / `AGENTS.md`。
3. コード、設定、API 契約、テスト、承認済みのアーキテクチャ文書など、現在のリポジトリで確認できるプロジェクトの事実。
4. [docs/ja/README.md](docs/ja/README.md) に記載された開発規約の全分冊。

[docs/ja/00-core-rules.md](docs/ja/00-core-rules.md) は、コーディングのたびに必ず確認する実行ルールであり、開発規約を実践するための要約でもある。標準の一部を省略したり、要件を緩和したりするものと解釈してはならない。

## 2. 必ず使用する 2 種類の React Native / Expo ルール文書

Codex は React Native / Expo のコードを扱うタスクで、次の 2 種類の共通ルール文書を使用しなければならない。

1. [docs/ja/00-core-rules.md](docs/ja/00-core-rules.md)
   - 簡潔な実行ルール。
   - React Native / Expo コードの生成、変更、修正、リファクタリングの前に、毎回全文を読まなければならない。

2. [docs/ja/README.md](docs/ja/README.md) に記載された開発規約の全分冊
   - 正式な開発規約の全文。
   - 本ファイルで指定する節を読む。アーキテクチャに関わるタスク、Feature をまたぐタスク、高リスクのタスクでは、`01`～`12` の全分冊を全文読まなければならない（`12` には原文の第 13 節のまとめも含まれる）。索引を分冊本文の代わりにしてはならない。

過去の会話や要約、記憶で代用したり、以前読んだことを理由に今回のタスクでの確認を省略したりしてはならない。コアルール、開発規約の索引、または今回のタスクで必読となる分冊が存在しないか読めない場合は、該当するパスを報告するまで React Native / Expo コードを変更してはならない。

## 3. コード変更前の必須手順

次の順序で実行しなければならない。手順 1～7 を完了するまで、コードの記述やファイルの変更を開始してはならない。

### 手順 1：現在のタスクを理解する

- 目的、制約、対象プラットフォーム、完了条件を明確にする。
- タスクの種類を判断する：機能実装、バグ修正、リファクタリング、コードレビュー、Figma デザインの再現、アーキテクチャ設計。
- ユーザーの明示的な要求とモデル自身の推測を区別し、推測をプロジェクトの事実として扱ってはならない。

### 手順 2：リポジトリのルールを読む

- ルートにある本ファイルを適用する。
- 対象ファイルまでのディレクトリを確認し、より近い `AGENTS.md` または `AGENTS.override.md` がないか調べる。
- 第 1 節の優先順位で競合を解決する。

### 手順 3：コアルールを全文読む

次を全文読む。

[docs/ja/00-core-rules.md](docs/ja/00-core-rules.md)

その中の 6 つの厳守ルールと完了前チェックリストを、今回のタスクの必須条件としなければならない。

### 手順 4：プロジェクトの実態を確認してからアーキテクチャを選ぶ

少なくとも次を確認する。

- `git status`。今回のタスクと無関係な変更を保持する。
- 関連するワークスペースの `package.json` と、使用可能なスクリプト。
- Expo 設定、`tsconfig`、パスエイリアス、lint、format、テストの設定。
- タスクに直接関係するアーキテクチャ文書、API 契約、プロジェクトの取り決め。
- 現在のプロジェクトで実際に採用されている状態管理、リクエスト、ストレージ、ルーティング、UI、テストの方式。

リポジトリ内の根拠なしに、依存関係、Route、デザイントークン、API 契約、状態管理ライブラリ、保存の仕組み、プラットフォームの動作を独断で決めてはならない。

### 手順 5：既存コードを確認してから新しいコードが必要か判断する

次を読み、検索しなければならない。

- 対象 Feature の Page、UI、Hook、Model、Use Case、API、Types、Utils。
- 対象コードの直接の呼び出し元、直接の利用側、関連テスト。
- 現在の Feature にある類似実装。
- `shared/ui`、`shared/hooks`、`shared/utils`、`shared/constants`、公開エクスポート。
- 既存のデザイントークン、基本コンポーネント、同じユーザーの意図を表す実装。

Shared コンポーネント、Shared Hook、共通フローを変更する前に、それらを直接利用する箇所をすべて確認しなければならない。現在のページだけを確認してはならない。

### 手順 6：タスクの種類に応じて開発規約を読む

索引：[docs/ja/README.md](docs/ja/README.md)。分冊番号は開発規約の原文の節番号に対応する。`12` には、原文の第 13 節のまとめも含まれる。

すべてのコードタスクで必ず読むセクション：

- 第 1 節：[ルールの位置づけと使用範囲](docs/ja/01-core-principles.md)
- 第 2 節：[プロジェクトとディレクトリ構造](docs/ja/02-project-structure.md)
- 第 4 節：[Page、UI、再利用、Controller、スタイル](docs/ja/04-component-and-styling.md)
- 第 7 節：[依存関係、変更範囲、成果物の引き渡し手順](docs/ja/07-delivery-and-constraints.md)
- 第 9 節：[テスト戦略](docs/ja/09-testing-strategy.md)
- 第 12 節：[最終チェックリスト](docs/ja/12-final-checklist.md)

タスクに応じて追加で読むセクション：

- Route、Navigation、Tabs、Stack、戻る動作、ルートレベルの Modal：[第 3 節](docs/ja/03-routing-and-navigation.md)を読む。
- Figma、新規 UI、見た目の調整、画像/SVG、フォーム、キーボード、Sheet、Dialog、Safe Area、アクセシビリティ、iOS/Android の差異：[第 5 節](docs/ja/05-figma-workflow.md)と[第 6 節](docs/ja/06-interaction-platform-and-accessibility.md)を読む。
- リスト、レンダリング、画像のパフォーマンス、アニメーション、キャッシュ、起動、メモリ、パフォーマンスの最適化：[第 8 節](docs/ja/08-performance-and-rendering.md)を読む。
- 認証、トークン、API セキュリティ、ストレージ、アップロード／ダウンロード、権限、Deep Link、WebView、プライバシー、機密データ：[第 10 節](docs/ja/10-security-and-privacy.md)を読む。
- プロジェクトの情報が不足している場合、プロジェクト内に新しい領域を立ち上げる場合、プロジェクト規約を監査する場合：[第 11 節](docs/ja/11-project-specific-rules.md)を読む。

次のタスクでは、開発規約全体、すなわち `01`～`12` の全分冊を全文読まなければならない。

- Feature 横断のリファクタリング。
- アーキテクチャの調整またはサブシステムの追加。
- Shared 基盤の変更。
- 認証／セッションの変更。
- セキュリティに関わる変更。
- 3 つ以上の本番モジュールに同時に影響する変更。

### 手順 7：実装前の品質ゲートを通過する

変更を開始する前に、少なくとも次を含む簡潔な実装方針を提示しなければならない。

1. 対象ファイルと各ファイルの責務。
2. 再利用候補と最終判断：そのまま再利用、拡張、より低い層の機能を抽出、独立した実装を維持。
3. 主要なユーザーアクションが UI コールバックから、処理全体を統括する唯一の箇所を経て、外部への副作用に至るまでの経路全体。
4. 使用予定のデザインパターン、そのパターンが解決する実際の複雑さ、不要なため導入しないパターンの明示。
5. 変更後に実行予定のチェックコマンドとプラットフォームの受け入れ確認項目。

再利用候補が見つからなかった場合は、検索した範囲と「意味が一致する実装は見つからなかった」ことを明記しなければならない。再利用の検討自体を省略してはならない。

## 4. 実装中の必須ルール

- 要件を漏れなく満たす最小限の変更を行い、無関係なコードを書き直さない。
- 新規作成の前に検索する。意味、状態に関する契約、アクセシビリティ／プラットフォームに関する契約、将来の変更理由が一致する場合だけ共通化する。
- 安定して再利用できる最も低い層を選ぶ：トークン → Primitive → 純粋関数/Model → Headless Hook → Feature Pattern → Shared Pattern。
- まだ安定していない抽象化は Feature 内に留め、ページ専用の真偽値、Route 判定、巨大な設定、無制限のスタイル上書きを含む万能コンポーネントを作らない。
- 主要なユーザーアクションごとに、処理全体を統括する箇所を 1 つだけ設ける。重要な手順は、その範囲内で順を追って読めなければならない。
- `handleX -> doX -> executeX -> service.x` のように処理を渡すだけの呼び出しチェーンは削除する。ただし、各層が実際に変換、検証、不変条件、分岐、エラーマッピング、並行制御、副作用の境界、差し替え可能な実装のいずれかを追加する場合を除く。
- 複雑さを解消する必要が実際に生じた場合だけ Controller、Use Case、Reducer/状態機械、Strategy、Adapter、Repository を使用する。アーキテクチャを整えているように見せるために層を増やしてはならない。
- UI は API、認証、ストレージ、ナビゲーションにまたがる処理の統括を担ってはならない。Use Case は React、UI、Toast、Router に依存してはならない。Reducer は純粋関数でなければならない。
- ユーザーの明示的な承認なしに、本番環境で使用する依存パッケージを追加しない。
- 共通コードを抽出する際は、同じ変更の中で対象の利用側を移行し、置き換えた重複実装を削除すべきである。段階的に行う必要がある場合は、範囲、理由、完了条件を記録する。
- 振る舞いとリグレッションリスクに応じて、対応するレイヤーのテストを追加または更新する。

## 5. 変更後の検証と再確認の順序

実装完了後は、次の順序で実行しなければならない。

1. プロジェクト既存のフォーマッターが使用可能な場合は、変更ファイルをフォーマットする。
2. 関連する Lint を実行する。
3. TypeScript Typecheck を実行する。
4. 変更した振る舞いに対するテストを先に実行し、その後、影響範囲に応じてより広い関連テスト群を実行する。
5. 影響範囲に応じて Expo config、bundle、ネイティブビルドのチェックを実行する。
6. プラットフォーム依存の変更は iOS と Android で検証しなければならず、Web レンダリングをネイティブの最終受け入れ確認にしてはならない。
7. 差分全体をレビューし、無関係な変更、デッドコード、残った重複実装、隠れた呼び出しチェーン、安全でない型アサーション、クリーンアップの漏れ、新たな層間の依存を確認する。
8. [コアルール第 6 節](docs/ja/00-core-rules.md)と[開発規約第 12 節](docs/ja/12-final-checklist.md)を読み直す。
9. 関連するすべてのチェック項目に、合格、不合格、対象外、未実行のいずれかを記録する。
10. 必須のチェックを実行していない場合は、該当する項目と理由を説明しなければならない。その状態で作業を「完全に完了した」と表現してはならない。

## 6. 最終回答に必ず含める内容

- 変更内容と主なファイル。
- 再利用／共通化の判断と理由。
- 主要アクションの経路と、処理全体を統括する箇所の責任範囲。
- 導入したデザインパターンとその必要性。
- 実際に実行したコマンド、チェック、結果。
- 完了した iOS / Android の検証。
- 残るリスク、差異、未検証事項。

## 7. 禁止事項

- 必須ルールと関連する既存コードを読まずに、コード生成を開始する。
- 再利用できる実装を検索せずに、類似の Button、Card、Row、Input、Modal、EmptyState、Hook、ユーティリティ、業務フローを追加する。
- コードの見た目が似ている、または 2 回登場したという理由だけで無理に抽象化する。
- EventBus、Context、Module Singleton、Generic Manager、同じ引数を渡すだけのラッパー関数を使い、直接表現できる制御フローを隠す。
- 明確な単一責務のない `helpers.ts`、`common.ts`、`manager.ts`、`service.ts` を作る。
- デザインパターンを使うこと自体を目的に、単純な 1 回の API 呼び出しに無意味な Use Case、Repository、Factory、クラス、インターフェースを追加する。
- 既存の技術スタックで明快に実装できる問題を、新たな依存関係で回避する。
- テスト、型チェック、両プラットフォームへの影響分析、差分レビューを省略して、タスク完了を宣言する。
